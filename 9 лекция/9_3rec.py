import itertools

buf = [[5, 7, 8, 9, 0], [5, 7, 8, 9, 0]]

spisok = []
subcode = ""
flags = [0] * len(buf)
j = len(buf) - 1

for i in range(len(buf)):
    for j in range()


#fu = list(itertools.combinations(buf, len(buf)))